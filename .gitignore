*.class
dependency-reduced-pom.xml
/workspace.xml
online-shop.iml
Test.java
TestClose.java
# Datasource local storage ignored files
/dataSources/
/dataSources.local.xml
R__setUpOptical.sql
src/main/resources/db/migration/R__setUpSun.sql

.idea
# Maven
target/
# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/shelfis not compatible with the current version of the IDE, bis not compatible with the current version of the IDE, bis not compatible with the current version of the IDE, bis not compatible with the current version of the IDE, because it requires build 202.* or older but the current build is IU-203.3645.34ecause it requires build 202.* or older but the current build is IU-203.3645.34ecause it requires build 202.* or older but the current build is IU-203.3645.34ecause it requires build 202.* or older but the current build is IU-203.3645.34